# Application bindings
bindd = SUPER ALT, RETURN, Tmux, exec, uwsm-app -- xdg-terminal-exec --dir="$(omarchy-cmd-terminal-cwd)" tmux new
$terminal = uwsm-app -- xdg-terminal-exec
$browser = omarchy-launch-browser



bindd = SUPER, RETURN, Terminal, exec, $terminal --working-directory="$(omarchy-cmd-terminal-cwd)"
bindd = SUPER , N, File manager, exec, uwsm-app -- nautilus --new-window
bindd = SUPER ALT SHIFT, F, File manager (cwd), exec, uwsm-app -- nautilus --new-window "$(omarchy-cmd-terminal-cwd)"
bindd = SUPER SHIFT, M, Music, exec, omarchy-launch-or-focus spotify
bindd = SUPER SHIFT, T, Activity, exec, omarchy-launch-tui btop
bindd = SUPER, O, Obsidian, exec, omarchy-launch-or-focus "^obsidian$" "uwsm-app -- obsidian -disable-gpu --enable-wayland-ime"
bindd = SUPER, B, Screen off, exec, hyprctl dispatch dpms off
# bindd = SUPER, B, Screen off, exec, hyprctl dispatch dpms off && sleep 1
# bind = SUPER, B, exec, sh -c "hyprctl dispatch dpms off; sleep 3"

# If your web app url contains #, type it as ## to prevent hyprland treating it as a comment
bindd = SUPER, A, Thorium , exec, thorium-browser
unbind = SUPER, W
bindd = SUPER, Q, Close, killactive

# Turn the internal display off when the lid closes, back on when it opens.
# Audio and everything else keep running (logind ignores the lid switch).
bindl = , switch:on:Lid Switch, exec, hyprctl dispatch dpms off eDP-1
bindl = , switch:off:Lid Switch, exec, hyprctl dispatch dpms on eDP-1

# Overwrite existing bindings, like putting Omarchy Menu on Super + Space
# unbind = SUPER, SPACE
# bindd = SUPER, SPACE, Omarchy menu, exec, omarchy-menu

# Super + Tab cycles tabs within a group (was: Next workspace)
unbind = SUPER, TAB
bindd = SUPER, TAB, Next tab in group, changegroupactive, f
bindd = SUPER SHIFT, TAB, Previous tab in group, changegroupactive, b
